;; -classe concreta: podemos instanciar objetos
;; -abstrata, tem o nome inclinado e é uma classe, (tem que ter pelo menos um método abstrato, mas pode ter metodos concretos, que não precisam ser chamados como as abstratos)(herança) e interface, é uma entidade por não ter métodos por isso implementamos, mas como no python tem herança múltipla podemos "herdar", 
;; importam outras classes por isso não são herdadas (na interfaçe só vamos ter métodos abstratos que srão importados nas classes concretas, assim como nas classes abtratas)
;; -pacote de classe abstrata para ter base e trabalhar com elas, abstractmethod (from abc import ABC, abstractmethod)
;; -as classes concretas que herdam de abstratas, logo, precisam ter seus métodos abstratos, sendo que esses métodos só poderão ser usados na classe que herdou e teremos que dizer como ele vai funcionar
se não quisermos usar os métodos de uma classe abstrata, que é obrigatório, podemos transformar a classe abstrata em concreta
